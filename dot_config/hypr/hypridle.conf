general {
    lock_cmd = pidof hyprlock || hyprlock       # Avoids starting multiple hyprlock instances.
    before_sleep_cmd = loginctl lock-session    # Locks the session before the system enters a sleep state.
    after_sleep_cmd = hyprctl dispatch 'hl.dsp.dpms({ action = "on" })'  # Explicitly wakes monitors after resuming from sleep.
}

listener {
    timeout = 300                               # 5 minutes (300 seconds)
    on-timeout = loginctl lock-session          # Use logind to lock the session. This sends a DBus signal that hypridle's lock_cmd is configured to catch, ensuring robust system integration.
}

listener {
    timeout = 360                               # 6 minutes (300s idle + 60s locked)
    on-timeout = hyprctl dispatch 'hl.dsp.dpms({ action = "off" })'      # Power down all displays.
    on-resume = hyprctl dispatch 'hl.dsp.dpms({ action = "on" })'        # Power displays back on immediately upon any activity.
}

# Listener 3: Suspend the system after 30 minutes of total inactivity.
# This is the final power-saving step for extended idle periods.
# listener {
#     timeout = 1800                              # 30 minutes (1800 seconds)
#     on-timeout = systemctl suspend              # Suspend the system to RAM.
# }
